# app_mobile (workspace wrapper)

Este directorio contiene un proyecto Flutter anidado.

Usa como app principal:

- `app_mobile/app_mobile`

Comandos recomendados:

```bash
cd /home/ale_siesta/app_ajedrez/app_mobile/app_mobile
flutter pub get
flutter analyze
flutter test
```
